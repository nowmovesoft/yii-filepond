# Codeception Test Suite Configuration
#
# Suite for unit or integration tests.

actor: UnitTester
modules:
    enabled:
        - Yii2:
            configFile: 'config/test.php'
            part: init
        - Asserts
        - \Helper\Unit
    step_decorators: ~
